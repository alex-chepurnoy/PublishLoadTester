# Default Configuration for Stream Load Tester
# This file contains default values and settings

# Stream Configuration
DEFAULT_BITRATE=2000
DEFAULT_DURATION=30
DEFAULT_CONNECTIONS=5
DEFAULT_RESOLUTION=1080p
DEFAULT_VIDEO_CODEC=h264
DEFAULT_AUDIO_CODEC=aac

# Limits
MAX_CONNECTIONS=1000
MAX_BITRATE=50000
MIN_BITRATE=100
MAX_DURATION=1440
MIN_DURATION=1

# Logging Configuration
LOG_LEVEL=INFO
LOG_ROTATION=true
MAX_LOG_SIZE=100M
MAX_LOG_FILES=10

# FFmpeg Configuration
FFMPEG_PRESET=veryfast
VIDEO_CODEC=libx264
VIDEO_SIZE=1920x1080
VIDEO_FPS=30
VIDEO_GOP=60
VIDEO_KEYINT_MIN=60
AUDIO_CODEC=aac
AUDIO_RATE=48000
AUDIO_BITRATE=128k

# Resolution Options
# Supported: 4k, 1080p, 720p, 360p
# Video Codec Options
# Supported: h264 (libx264), h265 (libx265)
# Audio Codec Options
# Supported: aac, opus

# Protocol Specific Settings
[RTMP]
DEFAULT_PORT=1935
CONNECTION_TIMEOUT=30

[RTSP]
DEFAULT_PORT=554
CONNECTION_TIMEOUT=30

[SRT]
DEFAULT_PORT=9999
CONNECTION_TIMEOUT=30
LATENCY=200

# System Resource Limits
MAX_MEMORY_PER_STREAM=100M
CPU_PRIORITY=normal

# Monitoring Settings
MONITOR_INTERVAL=10
HEALTH_CHECK_INTERVAL=30
STATISTICS_INTERVAL=60

# Retry Configuration
MAX_RETRIES=3
RETRY_DELAY=5
EXPONENTIAL_BACKOFF=true